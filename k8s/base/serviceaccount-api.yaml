apiVersion: v1
kind: ServiceAccount
metadata:
  name: api
  annotations:
    # Replace with: terraform -chdir=infra/environments/dev output -raw api_role_arn
    eks.amazonaws.com/role-arn: arn:aws:iam::000000000000:role/REPLACE_WITH_api_role_arn
