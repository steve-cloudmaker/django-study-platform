apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin
  labels:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/part-of: study-platform
type: Opaque
stringData:
  # Replace before production (kubectl create secret generic ... --from-literal=password=...)
  password: changeme
